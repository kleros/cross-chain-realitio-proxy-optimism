{
    "name": "@kleros/cross-chain-realitio-contracts",
    "version": "0.5.0",
    "main": "index.js",
    "author": "Kleros",
    "license": "MIT",
    "scripts": {
        "build": "hardhat compile",
        "test": "hardhat test",
        "start": "hardhat node --tags nop",
        "deploy-local": "hardhat deploy --network localhost --tags HomeChain && hardhat deploy --network localhost --tags ForeignChain",
        "deploy-staging": "hardhat deploy --network garnet && hardhat deploy --network holesky",
        "deploy-production": "hardhat deploy --network redstone && hardhat deploy --network mainnet"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
        "@nomicfoundation/hardhat-ethers": "^3.0.0",
        "@nomicfoundation/hardhat-ignition": "^0.15.5",
        "@nomicfoundation/hardhat-ignition-ethers": "^0.15.0",
        "@nomicfoundation/hardhat-network-helpers": "^1.0.11",
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "@nomicfoundation/hardhat-verify": "^2.0.0",
        "@nomicfoundation/ignition-core": "^0.15.5",
        "@typechain/ethers-v6": "^0.5.0",
        "@typechain/hardhat": "^9.0.0",
        "@types/chai": "^4.2.0",
        "@types/mocha": ">=9.1.0",
        "chai": "^4.2.0",
        "ethereum-waffle": "^4.0.10",
        "ethers": "^6.4.0",
        "hardhat": "^2.22.6",
        "hardhat-deploy": "^0.12.4",
        "hardhat-gas-reporter": "^1.0.8",
        "solidity-coverage": "^0.8.1",
        "ts-node": ">=8.0.0",
        "typechain": "^8.3.0",
        "typescript": ">=4.5.0"
    },
    "dependencies": {
        "@kleros/dispute-resolver-interface-contract": "^8.0.0",
        "dotenv": "^16.4.5"
    }
}
